run_id: YYYYMMDD_workload_gpu_seed_rep
git_commit: TBD
operator: TBD
start_utc: TBD
end_utc: TBD
workload:
  family: training
  name: TBD
  command: TBD
  dataset: TBD
  model: TBD
  precision: TBD
  batch_size: TBD
  sequence_length: TBD
  seed: 0
  useful_work_metric: samples_per_second
hardware:
  gpu_uuids: []
  scope_channels: []
  camera_rois: []
  contact_sensor_ids: []
  network_flow_scope: TBD
environment:
  container_digest: TBD
  driver: TBD
  cuda: TBD
  pytorch: TBD
controls:
  power_limit_W: TBD
  graphics_clock_MHz: TBD
  memory_clock_MHz: TBD
  ambient_inlet_C: TBD
sensors:
  nvml: true
  dcgm: true
  electrical: false
  thermal_camera: false
  contact_temperature: false
  ultrasound: false
  rf_sdr: false
  network_mirror: false
sensor_channels:
  # one entry per active channel; filled by the run supervisor
  - channel_id: TBD           # e.g. nvml.gpu0, pico0.chA
    sample_rate_hz: TBD
    clock_source: CLOCK_MONOTONIC_RAW
    alignment_error_ms: TBD   # measured against the run's load marker, never assumed
    missing_fraction: TBD
    health: TBD               # pass | fail; failing channels flag the run
profiled: false               # true only for Nsight/WAVE characterization passes, never for sensor corpus runs
useful_work_value: TBD        # measured value of workload.useful_work_metric
artifact_checksums: {}        # path -> sha256, filled at run close
status: planned
exclusion_reason: null
notes: null

